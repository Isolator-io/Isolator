/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Iso14496;

/**
 *
 * @author mac
 */
public class Backup {
    
}
/*
            if(n ==0){
                totalData[0][1] = sampleTableBox[0][1];
                //totalData[0][2] = sampleTableBox[0][1];
               // currentIndex++;
               //totalTestSamples += sampleTableBox[0][1];
               //totalTestChunks++;
            }else {
                
                while(currentIndex < sampleTableBox[n][0] ){
                    System.out.println(currentIndex);
                    totalData[currentIndex][0] = sampleTableBox[n][0];
                    //totalData[currentIndex][1] = sampleTableBox[n][1];
                    currentIndex++;
                }
                
                
                int chunksInRun = sampleTableBox[n][0] - sampleTableBox[n-1][0];
                totalTestChunks += chunksInRun;
                totalTestSamples += chunksInRun * sampleTableBox[n][1];
                System.out.println(chunksInRun);
                for(int c = 0; c < chunksInRun; c++){
                    //System.out.println(sampleTableBox[n][0] - c -1);
                    totalData[sampleTableBox[n][0] - c ][1] = sampleTableBox[n][1];
                    //totalData[sampleTableBox[n][0] - c ][2] = sampleTableBox[n][1];
                }
*/